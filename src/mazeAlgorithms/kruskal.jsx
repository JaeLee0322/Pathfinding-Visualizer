export function kruskal(grid) {
    let forest = [];
    grid.map(row => {
        row.map(col =>{
            forest.push(col);
        })
    })

    forset = forest.sort_by(rand);
}
export default kruskal